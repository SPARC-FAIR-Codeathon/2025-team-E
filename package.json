{
  "name": "toxic-trace-app",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd toxic-trace-web-app && npm install && npm run heroku-postbuild"
  },
  "engines": {
    "node": "18.17.1"
  }
}
